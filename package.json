{
  "name": "debug-overlay",
  "private": true,
  "description": "Debug Overlay — a screenshot-friendly UI inspector shipped as a browser extension. The shipped version number lives in release.json, not here — build.js owns it.",
  "scripts": {
    "check": "node build.js --same && node audit.js && node test.js",
    "ship": "node ship.js",
    "shipped": "node ship.js --status",
    "dev": "node development/serve.js",
    "build": "node build.js",
    "watch": "node build.js --watch",
    "audit": "node audit.js",
    "test": "node test.js",
    "map": "node map.js",
    "compare": "node compare.js",
    "mcp": "node mcp/"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "jsdom": "^30.0.1"
  }
}
